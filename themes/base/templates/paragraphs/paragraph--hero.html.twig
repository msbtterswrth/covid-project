{% set alignment = paragraph.content_field_text_alignment.value %}
<div class="hero">
    <div class="img">      
        {{ content.field_image }}
    </div>
    <div class="blurb {{alignment}}">
        <div class="container">
            <div class="inner">
                <h1>{{content.field_title.0}}</h1>
                {{ content.field_blurb }}
            </div>
        </div>
    </div>
</div>